import React from 'react';

type LoadingStateType = {}

function LoadingState(props: LoadingStateType) {
    return (
        <div>
            LoadingState
        </div>
    )
};

export default LoadingState;